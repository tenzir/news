id: tenzir-test
name: Tenzir Test
description: >-
  The integration testing framework that powers the Tenzir ecosystem. It
  discovers tests, runs them with managed fixtures, and validates output
  against baselines.
repository: tenzir/test
