This release streamlines the prompts for the `make_parser` and `make_ocsf_mapping` tools, reducing model errors and speeding up agent completion.

## 🔧 Changes

### Improved prompts

We streamlined the prompts for `make_parser` and `make_ocsf_mapping` tools to reduce the amount of errors that models make. This speeds up the completion of the agent.

*By @mavam.*
