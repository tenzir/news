This is the initial stable release of the TQL plugin. It provides a skill for building TQL pipelines with the Tenzir MCP server and uses tenzir-mcp@latest to ensure users always have the most recent MCP server version.

## 🚀 Features

### Add tql plugin

Provides a skill for building TQL pipelines with the Tenzir MCP server.

*By @mavam and @claude.*

## 🔧 Changes

### Update tql plugin to use tenzir-mcp@latest

Update tql plugin to use `tenzir-mcp@latest` to ensure users always have the latest MCP server version. This ensures compatibility with the most recent Tenzir MCP server features and improvements.

*By @mavam and @claude.*
